{% extends 'base.html.twig' %}

{% block body %}
  <h1>Connextion</h1>

  <form action="{{path('security_login')}}" method="post">
    <div class="form-group">
      <input placeholder="Adresse email" required name="_username" type="text" class="form-control">
    </div>
    <div class="form-group">
      <input placeholder="MDP" required name="_password" type="password" class="form-control">
    </div>
    <div>
      <button type="submit" class="btn btn-success"> Connexion </button>
    </div>
  </form>

  <div>
    <a href="{{path('security_inscription')}}"> Créer un compte </a>
  </div>


{% endblock %}
